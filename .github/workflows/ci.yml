name: CI - Validación Contador Web

on:
  push:
    branches: [main]  # Se ejecuta en cada push a main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v3

    - name: Simular validación de código
      run: echo "Todo correcto: Archivos encontrados y estructura válida."
